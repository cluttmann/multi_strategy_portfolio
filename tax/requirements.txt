# German Tax Engine Dependencies
# Install with: pip install -r tax/requirements.txt

alpaca-py>=0.21.0         # Alpaca Trading API SDK (modern version)
pandas>=2.0.0             # Data processing
gspread>=5.12.0           # Google Sheets API
oauth2client>=4.1.3       # Google auth (service account credentials)
python-dotenv>=1.0.0      # .env file loading
requests>=2.31.0          # HTTP client for ECB rate API (frankfurter.app)
