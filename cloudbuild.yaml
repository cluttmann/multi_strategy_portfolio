steps:

# ─────────────────────────────────────────────────────────────────────────────
# WAVE 0 — build two immutable source trees before any deploy runs.
#
# `torch` + `transformers` (FinBERT) are reachable ONLY from daily_regime_check:
# compute_regime_score is called at exactly one site inside it, the monthly regime
# buy only reads persisted state via regime_state(), and backfill_regime_scores
# skips the news signal by design. Shipping them in all 16 images took every image
# from 71 MB to 2.89 GB on 2026-05-10 (commit 282beed) = 45 GB of new images per
# deploy wave, which is what ran Artifact Registry to 272 GB / €16 per month.
#
# fn-light  = requirements.txt minus every line tagged `# ML-ONLY`  (15 functions)
# fn-heavy  = requirements.txt verbatim                            (daily_regime_check)
#
# These trees are written ONCE here and never mutated again. That matters: the
# deploy waves below run up to 6 steps CONCURRENTLY against the same /workspace,
# so rewriting a shared requirements.txt between steps would be a race in which a
# light deploy could pick up the heavy file.
# ─────────────────────────────────────────────────────────────────────────────

  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'prep-sources'
    entrypoint: 'bash'
    args:
    - -c
    - |
      set -euo pipefail
      rm -rf fn-light fn-heavy
      mkdir -p fn-light fn-heavy

      # main.py has no local module imports, so main.py + requirements.txt is a
      # complete deployable tree for every entry point.
      cp main.py fn-light/main.py
      cp main.py fn-heavy/main.py
      cp requirements.txt fn-heavy/requirements.txt
      # Strip only *dependency* lines carrying the tag — a leading '#' means the
      # line is documentation and must survive, or fn-light ends up with orphaned
      # comment fragments from the tag's own explanation block.
      grep -vE '^[^#].*# ML-ONLY' requirements.txt > fn-light/requirements.txt

      # Explicit empty ignore files: gcloud resolves .gcloudignore relative to the
      # source dir, and these trees must not inherit any repo-root exclusions.
      : > fn-light/.gcloudignore
      : > fn-heavy/.gcloudignore

      # Fail the build loudly rather than silently shipping the wrong deps.
      test -s fn-light/requirements.txt
      test -s fn-heavy/requirements.txt
      ! grep -qE '^(torch|transformers)' fn-light/requirements.txt
      grep -qE '^transformers' fn-heavy/requirements.txt
      grep -qE '^torch' fn-heavy/requirements.txt
      test -s fn-light/main.py
      test -s fn-heavy/main.py

      echo "--- fn-light deps (15 functions) ---"; cat fn-light/requirements.txt
    waitFor: ['-']

# ─────────────────────────────────────────────────────────────────────────────
# WAVE A — first 5 function deploys (run in parallel, no dependency).
#
# Cloud Build has a per-project concurrent-build quota; each `gcloud functions
# deploy` spawns ITS OWN inner Cloud Build to construct the container image.
# With 16 functions all set to `waitFor: ['-']`, the inner builds queue up,
# and the ones at the back of the queue EXPIRE after ~10 min, failing the
# whole parent build. Splitting deploys into 4 waves (A, B, C1, C2) caps peak
# concurrent inner builds at ~6, which fits comfortably under quota. (See Cloud Build
# failure 2026-05-17T15:35Z that triggered this refactor.)
# ─────────────────────────────────────────────────────────────────────────────

  # Step 1: Deploy the monthly_buy_hfea Function
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'deploy-hfea'
    args:
    - functions
    - deploy
    - monthly_buy_hfea
    - --source=fn-light
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=monthly_buy_hfea
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor: ['prep-sources']

  # Step 2: Deploy the rebalance_hfea Function
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'deploy-rebalance-hfea'
    args:
    - functions
    - deploy
    - rebalance_hfea
    - --source=fn-light
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=rebalance_hfea
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor: ['prep-sources']

  # Step 3: Deploy the monthly_buy_spxl Function
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'deploy-spxl'
    args:
    - functions
    - deploy
    - monthly_buy_spxl
    - --source=fn-light
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=monthly_buy_spxl
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor: ['prep-sources']

  # Step 4: Deploy the monthly_nine_sig_contributions Function
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'deploy-nine-sig-monthly'
    args:
    - functions
    - deploy
    - monthly_nine_sig_contributions
    - --source=fn-light
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=monthly_nine_sig_contributions
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor: ['prep-sources']

  # Step 5: Deploy the monthly_invest_all ORCHESTRATOR (runs all 7 monthly strategies)
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'deploy-orchestrator'
    args:
    - functions
    - deploy
    - monthly_invest_all
    - --source=fn-light
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=monthly_invest_all
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor: ['prep-sources']

# ─────────────────────────────────────────────────────────────────────────────
# WAVE B — next 6 function deploys (start after Wave A completes).
# ─────────────────────────────────────────────────────────────────────────────

  # Step 6: Deploy the monthly_dual_momentum Function
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'deploy-dm'
    args:
    - functions
    - deploy
    - monthly_dual_momentum
    - --source=fn-light
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=monthly_dual_momentum
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor:
      - 'deploy-hfea'
      - 'deploy-rebalance-hfea'
      - 'deploy-spxl'
      - 'deploy-nine-sig-monthly'
      - 'deploy-orchestrator'

  # Steps 4e + 4f (monthly_buy_rssb_wtip + rebalance_rssb_wtip) removed 2026-05-12 —
  # RSSB/WTIP strategy discontinued for poor performance. The deployed Cloud Functions
  # and the Cloud Scheduler job will be deleted from GCP separately (see ops cleanup).

  # Step 7: Deploy the quarterly_nine_sig_signal Function
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'deploy-nine-sig-quarterly'
    args:
    - functions
    - deploy
    - quarterly_nine_sig_signal
    - --source=fn-light
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=quarterly_nine_sig_signal
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor:
      - 'deploy-hfea'
      - 'deploy-rebalance-hfea'
      - 'deploy-spxl'
      - 'deploy-nine-sig-monthly'
      - 'deploy-orchestrator'

  # Step 8: Deploy the daily_trade_spxl_200sma Function
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'deploy-spxl-200sma'
    args:
    - functions
    - deploy
    - daily_trade_spxl_200sma
    - --source=fn-light
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=daily_trade_spxl_200sma
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor:
      - 'deploy-hfea'
      - 'deploy-rebalance-hfea'
      - 'deploy-spxl'
      - 'deploy-nine-sig-monthly'
      - 'deploy-orchestrator'

  # Step 9: Deploy daily_regime_check (SSO/USFR regime detector daily run).
  # Memory bumped to 2GB to fit FinBERT (~440MB at runtime) plus the 500-stock
  # market breadth scan in a single invocation.
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'deploy-regime-check'
    args:
    - functions
    - deploy
    - daily_regime_check
    - --source=fn-heavy      # the ONLY function that needs torch/transformers
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=daily_regime_check_route
    - --memory=2048MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor:
      - 'deploy-hfea'
      - 'deploy-rebalance-hfea'
      - 'deploy-spxl'
      - 'deploy-nine-sig-monthly'
      - 'deploy-orchestrator'

  # Step 10: Deploy monthly_buy_regime_sso (regime detector monthly contribution)
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'deploy-regime-sso'
    args:
    - functions
    - deploy
    - monthly_buy_regime_sso
    - --source=fn-light
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=monthly_buy_regime_sso
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor:
      - 'deploy-hfea'
      - 'deploy-rebalance-hfea'
      - 'deploy-spxl'
      - 'deploy-nine-sig-monthly'
      - 'deploy-orchestrator'

  # Step 11: Deploy backfill_regime_scores (one-shot history seeder, manually triggered)
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'deploy-regime-backfill'
    args:
    - functions
    - deploy
    - backfill_regime_scores
    - --source=fn-light
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=backfill_regime_scores_route
    - --memory=1024MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor:
      - 'deploy-hfea'
      - 'deploy-rebalance-hfea'
      - 'deploy-spxl'
      - 'deploy-nine-sig-monthly'
      - 'deploy-orchestrator'

  # Steps 5e + 5f + 5g (daily_regime_world_check + monthly_buy_regime_world + backfill_regime_world_scores)
  # removed 2026-05-12 — Regime World strategy discontinued, replaced by World 40/30/30 (F4) +
  # 7-Asset Rotator (AAA). The deployed Cloud Functions and the daily_regime_world_check
  # Cloud Scheduler job have been deleted from GCP (cleanup completed 2026-05-17).

# ─────────────────────────────────────────────────────────────────────────────
# WAVE C1 — final strategy deploy after Wave B completes.
# ─────────────────────────────────────────────────────────────────────────────

  # Step 12: Deploy monthly_buy_aaa (7-Asset Rotator — momentum rotation across NTSD/SAA/EET/UBT/UST/UGL/DBC).
  # Called in-process by the monthly_invest_all orchestrator; deployed individually for manual/debug use.
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'deploy-aaa'
    args:
    - functions
    - deploy
    - monthly_buy_aaa
    - --source=fn-light
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=monthly_buy_aaa
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor:
      - 'deploy-dm'
      - 'deploy-nine-sig-quarterly'
      - 'deploy-spxl-200sma'
      - 'deploy-regime-check'
      - 'deploy-regime-sso'
      - 'deploy-regime-backfill'

# ─────────────────────────────────────────────────────────────────────────────
# WAVE C2 — final 2 function deploys (start after Wave C1 completes).
# ─────────────────────────────────────────────────────────────────────────────

  # Step 15: Deploy the unified index_alert Cloud Function with Alpaca and Telegram settings
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'deploy-index-alert'
    args:
    - functions
    - deploy
    - index_alert
    - --source=fn-light
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=index_alert
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor:
      - 'deploy-aaa'

  # Step 16: Deploy the audit_monthly_run watchdog (verifies the monthly orchestrator actually ran)
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'deploy-audit'
    args:
    - functions
    - deploy
    - audit_monthly_run
    - --source=fn-light
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=audit_monthly_run_route
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor:
      - 'deploy-aaa'

# ─────────────────────────────────────────────────────────────────────────────
# WAVE D — Cloud Scheduler (re)creation. Schedulers don't trigger inner Cloud
# Builds, so they can all run in parallel once Wave C is done. Gated on the
# last Wave C step so a scheduler is never created pointing at a function URL
# that hasn't been deployed yet.
# ─────────────────────────────────────────────────────────────────────────────

  # Cloud Scheduler job for 'monthly_invest_all' ORCHESTRATOR (runs all 6 monthly strategies)
  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gcloud scheduler jobs update http monthly_invest_all \
          --schedule='0 12 1-7 * *' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/monthly_invest_all' \
          --http-method='POST' \
          --time-zone='America/New_York' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
        || gcloud scheduler jobs create http monthly_invest_all \
          --schedule='0 12 1-7 * *' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/monthly_invest_all' \
          --http-method='POST' \
          --time-zone='America/New_York' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com'
    waitFor:
      - 'deploy-aaa'
      - 'deploy-index-alert'
      - 'deploy-audit'

  # Cloud Scheduler job for 'rebalance_hfea' function
  - name: 'gcr.io/cloud-builders/gcloud'
    args: [
      'scheduler', 'jobs', 'update', 'http', 'rebalance_hfea',
      '--schedule', '0 14 1-7 1,4,7,10 *',
      '--uri', 'https://europe-west3-trading-436516.cloudfunctions.net/rebalance_hfea',
      '--http-method', 'POST',
      '--time-zone', 'America/New_York',
      '--location', 'europe-west3',
      '--oidc-service-account-email', '1098661711782-compute@developer.gserviceaccount.com'
    ]
    waitFor:
      - 'deploy-aaa'
      - 'deploy-index-alert'
      - 'deploy-audit'

  # Cloud Scheduler job 'rebalance_rssb_wtip' removed 2026-05-12 — RSSB/WTIP strategy
  # discontinued. The existing GCP scheduler job has been deleted (cleanup completed 2026-05-17).

  # NOTE: monthly_buy_spxl, monthly_nine_sig_contributions, monthly_buy_hfea, monthly_dual_momentum,
  # and monthly_buy_aaa schedulers are intentionally absent — they have all been
  # REPLACED by the monthly_invest_all orchestrator for coordinated budget management.
  # The individual functions are deployed for manual/debugging use but never directly scheduled.

  # Cloud Scheduler job for 'quarterly_nine_sig_signal' function
  - name: 'gcr.io/cloud-builders/gcloud'
    args: [
      'scheduler', 'jobs', 'update', 'http', 'quarterly_nine_sig_signal',
      '--schedule', '0 13 1-7 1,4,7,10 *',
      '--uri', 'https://europe-west3-trading-436516.cloudfunctions.net/quarterly_nine_sig_signal',
      '--http-method', 'POST',
      '--time-zone', 'America/New_York',
      '--location', 'europe-west3',
      '--oidc-service-account-email', '1098661711782-compute@developer.gserviceaccount.com'
    ]
    waitFor:
      - 'deploy-aaa'
      - 'deploy-index-alert'
      - 'deploy-audit'

  # Cloud Scheduler job for 'daily_trade_spxl_200sma' function
  - name: 'gcr.io/cloud-builders/gcloud'
    args: [
      'scheduler', 'jobs', 'update', 'http', 'daily_trade_spxl_200sma',
      '--schedule', '56 15 * * 1-5',
      '--uri', 'https://europe-west3-trading-436516.cloudfunctions.net/daily_trade_spxl_200sma',
      '--http-method', 'POST',
      '--time-zone', 'America/New_York',
      '--location', 'europe-west3',
      '--oidc-service-account-email', '1098661711782-compute@developer.gserviceaccount.com'
    ]
    waitFor:
      - 'deploy-aaa'
      - 'deploy-index-alert'
      - 'deploy-audit'

  # Cloud Scheduler Job for S&P 500 ATH Drop Alert
  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gcloud scheduler jobs update http sp500_drop_alert \
          --schedule='30 9-15 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='America/New_York' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "SPY", "index_name": "S&P 500 (SPY)", "alert_type": "ath_drop", "threshold_percent": 30.0}' \
        || gcloud scheduler jobs create http sp500_drop_alert \
          --schedule='30 9-15 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='America/New_York' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "SPY", "index_name": "S&P 500 (SPY)", "alert_type": "ath_drop", "threshold_percent": 30.0}'
    waitFor:
      - 'deploy-aaa'
      - 'deploy-index-alert'
      - 'deploy-audit'

  # Cloud Scheduler Job for MSCI World ATH Drop Alert
  - name: 'gcr.io/cloud-builders/gcloud'
    args: [
      'scheduler', 'jobs', 'update', 'http', 'msci_drop_alert',
      '--schedule', '30 9-15 * * 1-5',
      '--uri', 'https://europe-west3-trading-436516.cloudfunctions.net/index_alert',
      '--http-method', 'POST',
      '--time-zone', 'America/New_York',
      '--location', 'europe-west3',
      '--oidc-service-account-email', '1098661711782-compute@developer.gserviceaccount.com',
      '--message-body', '{"index_symbol": "URTH", "index_name": "MSCI World", "alert_type": "ath_drop", "threshold_percent": 30.0}'
    ]
    waitFor:
      - 'deploy-aaa'
      - 'deploy-index-alert'
      - 'deploy-audit'

  # Cloud Scheduler Job for URTH 255-day SMA crossing alert
  # ─────────────────────────────────────────────────────────────────────────
  # RETIRED 2026-09-07: urth_255sma_alert
  #
  # Ersetzt durch world_eur_sma_* am Ende dieser Wave. Der alte Alert mass URTH
  # in USD zu New Yorker Zeiten, die Handelsentscheidung faellt aber in EUR an
  # der XETRA um 17:30 CET - zwei verschiedene Zeitscheiben fuer dieselbe
  # Entscheidung.
  #
  # ACHTUNG: cloudbuild kennt nur create/update. Das Entfernen dieses Blocks
  # loescht den Job NICHT. Einmalig manuell noetig:
  #   gcloud scheduler jobs delete urth_255sma_alert --location=europe-west3
  #
  # msci_drop_alert bleibt bestehen - anderer alert_type (ATH-Drop/Kreditsignal).
  # ─────────────────────────────────────────────────────────────────────────

  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gcloud scheduler jobs update http spy_200sma_alert \
          --schedule='15 9-15 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='America/New_York' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "SPY", "index_name": "S&P 500 (SPY)", "alert_type": "sma_crossing", "sma_period": 200, "noise_threshold": 1.0}' \
        || gcloud scheduler jobs create http spy_200sma_alert \
          --schedule='15 9-15 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='America/New_York' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "SPY", "index_name": "S&P 500 (SPY)", "alert_type": "sma_crossing", "sma_period": 200, "noise_threshold": 1.0}'
    waitFor:
      - 'deploy-aaa'
      - 'deploy-index-alert'
      - 'deploy-audit'

  # Cloud Scheduler job for the monthly run audit watchdog.
  # Runs once on day 8 of each month at 14:00 ET — after the day-1-7 buy window —
  # so a missed orchestrator run produces a Telegram alert.
  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        if gcloud scheduler jobs describe audit_monthly_run --location=europe-west3 >/dev/null 2>&1; then
          gcloud scheduler jobs update http audit_monthly_run \
            --schedule='0 14 8 * *' \
            --uri='https://europe-west3-trading-436516.cloudfunctions.net/audit_monthly_run' \
            --http-method='POST' \
            --time-zone='America/New_York' \
            --location='europe-west3' \
            --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com'
        else
          gcloud scheduler jobs create http audit_monthly_run \
            --schedule='0 14 8 * *' \
            --uri='https://europe-west3-trading-436516.cloudfunctions.net/audit_monthly_run' \
            --http-method='POST' \
            --time-zone='America/New_York' \
            --location='europe-west3' \
            --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com'
        fi
    waitFor:
      - 'deploy-aaa'
      - 'deploy-index-alert'
      - 'deploy-audit'

  # Cloud Scheduler for the daily regime detection check (SSO/USFR).
  # Runs every weekday at 4:30 PM ET — 30 min after market close so end-of-day
  # data is settled. Trades execute next morning if a rotation triggers.
  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        if gcloud scheduler jobs describe daily_regime_check --location=europe-west3 >/dev/null 2>&1; then
          gcloud scheduler jobs update http daily_regime_check \
            --schedule='30 16 * * 1-5' \
            --uri='https://europe-west3-trading-436516.cloudfunctions.net/daily_regime_check' \
            --http-method='POST' \
            --time-zone='America/New_York' \
            --location='europe-west3' \
            --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
            --attempt-deadline='300s'
        else
          gcloud scheduler jobs create http daily_regime_check \
            --schedule='30 16 * * 1-5' \
            --uri='https://europe-west3-trading-436516.cloudfunctions.net/daily_regime_check' \
            --http-method='POST' \
            --time-zone='America/New_York' \
            --location='europe-west3' \
            --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
            --attempt-deadline='300s'
        fi
    waitFor:
      - 'deploy-aaa'
      - 'deploy-index-alert'
      - 'deploy-audit'

  # Cloud Scheduler 'daily_regime_world_check' removed 2026-05-12 — Regime World strategy
  # discontinued. The existing GCP scheduler job has been deleted (cleanup completed 2026-05-17).

  # F4 retired 2026-09-09. Keep this cleanup idempotent so every later build
  # confirms the two obsolete Functions and quarterly scheduler stay absent.
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'retire-f4-services'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gcloud scheduler jobs delete quarterly_rebalance_f4 \
          --location=europe-west3 --quiet 2>/dev/null || true
        gcloud functions delete monthly_buy_f4 \
          --region=europe-west3 --quiet 2>/dev/null || true
        gcloud functions delete quarterly_rebalance_f4 \
          --region=europe-west3 --quiet 2>/dev/null || true
    waitFor:
      - 'deploy-aaa'
      - 'deploy-index-alert'
      - 'deploy-audit'


  # ─────────────────────────────────────────────────────────────────────────
  # EUR-SMA-Alerts auf der XETRA-Zeitscheibe (2026-09-07)
  #
  # Ersetzen urth_255sma_alert. Gegatet werden die beiden gehebelten
  # Euro-Produkte (2x ACWI LU3386643970, 2x World) - das Signal laeuft aber auf
  # dem UNGEHEBELTEN 1x-Tracker, so rechnet auch die Studie.
  #
  # Zeitzone Europe/Berlin, NICHT America/New_York wie die aelteren Alerts: der
  # Anker ist der XETRA-Schluss um 17:30, und die Sommerzeitumstellungen der
  # beiden Zonen sind nicht deckungsgleich.
  #
  # Drei Rollen pro Symbol:
  #   advisory  0 15-17  Vorwarnung, schreibt bewusst keinen State
  #   decisive  20 17    Handelsalarm, 10 Min vor Handelsschluss
  #   reconcile 45 17    Abgleich auf den echten Schluss + taeglicher Lebendtest
  #
  # Fenster: ACWI 250d, World 255d. 250d ist fuer den ACWI die gemessene Zahl -
  # nach deutscher Steuer 13,17 % CAGR / Sharpe 0,402 gegen 12,68 % / 0,379 bei
  # 255d (acwi_eur_final.json), und ab 1988 ebenfalls das Optimum des langsamen
  # Plateaus. World behaelt die 255d des ersetzten urth_255sma_alert.
  #
  # Nur der ACWI-decisive-Job traegt public_chat_secret: der Crossing-Alert geht
  # zusaetzlich in einen zweiten Telegram-Kanal (Freunde). Bewusst nur echte
  # Crossings - Vorwarnungen, reconcile-Korrekturen und Datenfehler bleiben im
  # privaten Kanal.
  #
  # Kein neuer Deploy-Schritt: index_alert nimmt den neuen Pfad ueber den
  # source-Parameter, die Function-Liste bleibt unveraendert.
  # ─────────────────────────────────────────────────────────────────────────

  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gcloud scheduler jobs update http acwi_eur_sma_advisory \
          --schedule='0 15-17 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='Europe/Berlin' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "IUSQ.XETRA", "index_name": "MSCI ACWI (EUR)", "alert_type": "sma_crossing", "source": "eodhd", "run_role": "advisory", "sma_period": 250, "noise_threshold": 1.0, "currency_symbol": "€"}' \
        || gcloud scheduler jobs create http acwi_eur_sma_advisory \
          --schedule='0 15-17 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='Europe/Berlin' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "IUSQ.XETRA", "index_name": "MSCI ACWI (EUR)", "alert_type": "sma_crossing", "source": "eodhd", "run_role": "advisory", "sma_period": 250, "noise_threshold": 1.0, "currency_symbol": "€"}'
    waitFor:
      - 'deploy-aaa'
      - 'deploy-index-alert'
      - 'deploy-audit'

  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gcloud scheduler jobs update http acwi_eur_sma_decisive \
          --schedule='20 17 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='Europe/Berlin' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "IUSQ.XETRA", "index_name": "MSCI ACWI (EUR)", "alert_type": "sma_crossing", "source": "eodhd", "run_role": "decisive", "sma_period": 250, "noise_threshold": 1.0, "currency_symbol": "€", "public_chat_secret": "TELEGRAM_CHAT_ID_ACWI"}' \
        || gcloud scheduler jobs create http acwi_eur_sma_decisive \
          --schedule='20 17 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='Europe/Berlin' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "IUSQ.XETRA", "index_name": "MSCI ACWI (EUR)", "alert_type": "sma_crossing", "source": "eodhd", "run_role": "decisive", "sma_period": 250, "noise_threshold": 1.0, "currency_symbol": "€", "public_chat_secret": "TELEGRAM_CHAT_ID_ACWI"}'
    waitFor:
      - 'deploy-aaa'
      - 'deploy-index-alert'
      - 'deploy-audit'

  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gcloud scheduler jobs update http acwi_eur_sma_reconcile \
          --schedule='45 17 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='Europe/Berlin' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "IUSQ.XETRA", "index_name": "MSCI ACWI (EUR)", "alert_type": "sma_crossing", "source": "eodhd", "run_role": "reconcile", "sma_period": 250, "noise_threshold": 1.0, "currency_symbol": "€"}' \
        || gcloud scheduler jobs create http acwi_eur_sma_reconcile \
          --schedule='45 17 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='Europe/Berlin' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "IUSQ.XETRA", "index_name": "MSCI ACWI (EUR)", "alert_type": "sma_crossing", "source": "eodhd", "run_role": "reconcile", "sma_period": 250, "noise_threshold": 1.0, "currency_symbol": "€"}'
    waitFor:
      - 'deploy-aaa'
      - 'deploy-index-alert'
      - 'deploy-audit'

  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gcloud scheduler jobs update http world_eur_sma_advisory \
          --schedule='0 15-17 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='Europe/Berlin' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "EUNL.XETRA", "index_name": "MSCI World (EUR)", "alert_type": "sma_crossing", "source": "eodhd", "run_role": "advisory", "sma_period": 255, "noise_threshold": 1.0, "currency_symbol": "€"}' \
        || gcloud scheduler jobs create http world_eur_sma_advisory \
          --schedule='0 15-17 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='Europe/Berlin' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "EUNL.XETRA", "index_name": "MSCI World (EUR)", "alert_type": "sma_crossing", "source": "eodhd", "run_role": "advisory", "sma_period": 255, "noise_threshold": 1.0, "currency_symbol": "€"}'
    waitFor:
      - 'deploy-aaa'
      - 'deploy-index-alert'
      - 'deploy-audit'

  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gcloud scheduler jobs update http world_eur_sma_decisive \
          --schedule='20 17 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='Europe/Berlin' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "EUNL.XETRA", "index_name": "MSCI World (EUR)", "alert_type": "sma_crossing", "source": "eodhd", "run_role": "decisive", "sma_period": 255, "noise_threshold": 1.0, "currency_symbol": "€"}' \
        || gcloud scheduler jobs create http world_eur_sma_decisive \
          --schedule='20 17 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='Europe/Berlin' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "EUNL.XETRA", "index_name": "MSCI World (EUR)", "alert_type": "sma_crossing", "source": "eodhd", "run_role": "decisive", "sma_period": 255, "noise_threshold": 1.0, "currency_symbol": "€"}'
    waitFor:
      - 'deploy-aaa'
      - 'deploy-index-alert'
      - 'deploy-audit'

  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gcloud scheduler jobs update http world_eur_sma_reconcile \
          --schedule='45 17 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='Europe/Berlin' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "EUNL.XETRA", "index_name": "MSCI World (EUR)", "alert_type": "sma_crossing", "source": "eodhd", "run_role": "reconcile", "sma_period": 255, "noise_threshold": 1.0, "currency_symbol": "€"}' \
        || gcloud scheduler jobs create http world_eur_sma_reconcile \
          --schedule='45 17 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='Europe/Berlin' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "EUNL.XETRA", "index_name": "MSCI World (EUR)", "alert_type": "sma_crossing", "source": "eodhd", "run_role": "reconcile", "sma_period": 255, "noise_threshold": 1.0, "currency_symbol": "€"}'
    waitFor:
      - 'deploy-aaa'
      - 'deploy-index-alert'
      - 'deploy-audit'

# Overall build timeout 60min: 4 deploy waves (A, B, C1, C2) × ~3-5min each + scheduler wave = ~25-30min total.
timeout: '3600s'

options:
  logging: CLOUD_LOGGING_ONLY  # Send logs to Cloud Logging only
