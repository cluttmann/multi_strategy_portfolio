steps:
  #Step 1: Deploy the monthly_buy_hfeaFunction
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
    - functions
    - deploy
    - monthly_buy_hfea  # The name of the Cloud Function for monthly buys
    - --source=.
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=monthly_buy_hfea  # Entry point function in your Python code
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  #Step 2: Deploy the rebalance_hfeaFunction
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
    - functions
    - deploy
    - rebalance_hfea  # The name of the Cloud Function for rebalancing
    - --source=.
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=rebalance_hfea  # Entry point function in your Python code
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  #Step 2a: Deploy the monthly_buy_golden_hfea_liteFunction
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
    - functions
    - deploy
    - monthly_buy_golden_hfea_lite  # The name of the Cloud Function for monthly Golden HFEA Lite buys
    - --source=.
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=monthly_buy_golden_hfea_lite  # Entry point function in your Python code
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  #Step 2b: Deploy the rebalance_golden_hfea_liteFunction
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
    - functions
    - deploy
    - rebalance_golden_hfea_lite  # The name of the Cloud Function for Golden HFEA Lite rebalancing
    - --source=.
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=rebalance_golden_hfea_lite  # Entry point function in your Python code
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  # Step 3: Deploy the monthly_buy_spxl
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
    - functions
    - deploy
    - monthly_buy_spxl  # The name of the Cloud Function for rebalancing
    - --source=.
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=monthly_buy_spxl  # Entry point function in your Python code
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  # Step 4: Deploy the monthly_nine_sig_contributions
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
    - functions
    - deploy
    - monthly_nine_sig_contributions  # The name of the Cloud Function for 9-sig monthly contributions
    - --source=.
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=monthly_nine_sig_contributions  # Entry point function in your Python code
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  # Step 4b: Deploy the monthly_invest_all ORCHESTRATOR (runs all 4 monthly strategies)
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
    - functions
    - deploy
    - monthly_invest_all  # Orchestrator function for coordinated monthly investing
    - --source=.
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=monthly_invest_all  # Entry point function in your Python code
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  # Step 4c: Deploy the monthly_dual_momentum function
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
    - functions
    - deploy
    - monthly_dual_momentum  # Dual Momentum Strategy function
    - --source=.
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=monthly_dual_momentum  # Entry point function in your Python code
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  # Step 4d: Deploy the monthly_sector_momentum function
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
    - functions
    - deploy
    - monthly_sector_momentum  # Sector Momentum Strategy function
    - --source=.
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=monthly_sector_momentum  # Entry point function in your Python code
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  # Step 4e: Deploy the monthly_buy_rssb_wtip function
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
    - functions
    - deploy
    - monthly_buy_rssb_wtip  # RSSB/WTIP Strategy monthly investment function
    - --source=.
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=monthly_buy_rssb_wtip  # Entry point function in your Python code
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  # Step 4f: Deploy the rebalance_rssb_wtip function
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
    - functions
    - deploy
    - rebalance_rssb_wtip  # RSSB/WTIP Strategy quarterly rebalancing function
    - --source=.
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=rebalance_rssb_wtip  # Entry point function in your Python code
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  # Step 5: Deploy the quarterly_nine_sig_signal
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
    - functions
    - deploy
    - quarterly_nine_sig_signal  # The name of the Cloud Function for 9-sig quarterly signals
    - --source=.
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=quarterly_nine_sig_signal  # Entry point function in your Python code
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  # Step 4: Deploy the daily_trade_spxl_200sma
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
    - functions
    - deploy
    - daily_trade_spxl_200sma  # The name of the Cloud Function for rebalancing
    - --source=.
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=daily_trade_spxl_200sma  # Entry point function in your Python code
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  # Step 6: Deploy the unified index_alert Cloud Function with Alpaca and Telegram settings
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
    - functions
    - deploy
    - index_alert  # Single Cloud Function for all indices
    - --source=.
    - --trigger-http
    - --region=europe-west3
    - --runtime=python310
    - --timeout=540s
    - --entry-point=index_alert  # Python function entry point
    - --memory=512MB
    - --set-env-vars=GOOGLE_CLOUD_PROJECT=1098661711782,GOOGLE_CLOUD_PROJECT_ID=trading-436516
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential


# Create Cloud Scheduler job for 'monthly_invest_all' ORCHESTRATOR (runs all 4 monthly strategies)
  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gcloud scheduler jobs update http monthly_invest_all \
          --schedule='0 12 1-7 * *' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/monthly_invest_all' \
          --http-method='POST' \
          --time-zone='America/New_York' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
        || gcloud scheduler jobs create http monthly_invest_all \
          --schedule='0 12 1-7 * *' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/monthly_invest_all' \
          --http-method='POST' \
          --time-zone='America/New_York' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com'
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  # Create Cloud Scheduler job for 'rebalance_hfea' function
  - name: 'gcr.io/cloud-builders/gcloud'
    args: [
      'scheduler', 'jobs', 'update', 'http', 'rebalance_hfea',
      '--schedule', '0 14 1-7 1,4,7,10 *',  
      '--uri', 'https://europe-west3-trading-436516.cloudfunctions.net/rebalance_hfea',
      '--http-method', 'POST',
      '--time-zone', 'America/New_York',
      '--location', 'europe-west3',
      '--oidc-service-account-email', '1098661711782-compute@developer.gserviceaccount.com'
    ]
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  # Create Cloud Scheduler job for 'rebalance_golden_hfea_lite' function (try update first, then create if needed)
  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        if gcloud scheduler jobs describe rebalance_golden_hfea_lite --location=europe-west3 >/dev/null 2>&1; then
          gcloud scheduler jobs update http rebalance_golden_hfea_lite \
            --schedule='0 14 1-7 1,4,7,10 *' \
            --uri='https://europe-west3-trading-436516.cloudfunctions.net/rebalance_golden_hfea_lite' \
            --http-method='POST' \
            --time-zone='America/New_York' \
            --location='europe-west3' \
            --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com'
        else
          gcloud scheduler jobs create http rebalance_golden_hfea_lite \
            --schedule='0 14 1-7 1,4,7,10 *' \
            --uri='https://europe-west3-trading-436516.cloudfunctions.net/rebalance_golden_hfea_lite' \
            --http-method='POST' \
            --time-zone='America/New_York' \
            --location='europe-west3' \
            --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com'
        fi
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  # Create Cloud Scheduler job for 'rebalance_rssb_wtip' function (try update first, then create if needed)
  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        if gcloud scheduler jobs describe rebalance_rssb_wtip --location=europe-west3 >/dev/null 2>&1; then
          gcloud scheduler jobs update http rebalance_rssb_wtip \
            --schedule='0 14 1-7 1,4,7,10 *' \
            --uri='https://europe-west3-trading-436516.cloudfunctions.net/rebalance_rssb_wtip' \
            --http-method='POST' \
            --time-zone='America/New_York' \
            --location='europe-west3' \
            --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com'
        else
          gcloud scheduler jobs create http rebalance_rssb_wtip \
            --schedule='0 14 1-7 1,4,7,10 *' \
            --uri='https://europe-west3-trading-436516.cloudfunctions.net/rebalance_rssb_wtip' \
            --http-method='POST' \
            --time-zone='America/New_York' \
            --location='europe-west3' \
            --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com'
        fi
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  # NOTE: monthly_buy_spxl, monthly_nine_sig_contributions, monthly_buy_hfea, and monthly_dual_momentum schedulers
  # have been REPLACED by the monthly_invest_all orchestrator for coordinated budget management.
  # The individual functions are still deployed for manual/debugging use but not scheduled.

  # Create Cloud Scheduler job for 'quarterly_nine_sig_signal' function
  - name: 'gcr.io/cloud-builders/gcloud'
    args: [
      'scheduler', 'jobs', 'update', 'http', 'quarterly_nine_sig_signal',
      '--schedule', '0 13 1-7 1,4,7,10 *',  
      '--uri', 'https://europe-west3-trading-436516.cloudfunctions.net/quarterly_nine_sig_signal',
      '--http-method', 'POST',
      '--time-zone', 'America/New_York',
      '--location', 'europe-west3',
      '--oidc-service-account-email', '1098661711782-compute@developer.gserviceaccount.com'
    ]
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  # Create Cloud Scheduler job for 'buy_spxl_above_200sma' function
  - name: 'gcr.io/cloud-builders/gcloud'
    args: [
      'scheduler', 'jobs', 'update', 'http', 'daily_trade_spxl_200sma',
      '--schedule', '56 15 * * 1-5',  
      '--uri', 'https://europe-west3-trading-436516.cloudfunctions.net/daily_trade_spxl_200sma',
      '--http-method', 'POST',
      '--time-zone', 'America/New_York',
      '--location', 'europe-west3',
      '--oidc-service-account-email', '1098661711782-compute@developer.gserviceaccount.com'
    ]
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  # Cloud Scheduler Job for S&P 500 ATH Drop Alert (try update first, then create if needed)
  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gcloud scheduler jobs update http sp500_drop_alert \
          --schedule='30 9-15 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='America/New_York' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "SPY", "index_name": "S&P 500 (SPY)", "alert_type": "ath_drop", "threshold_percent": 30.0}' \
        || gcloud scheduler jobs create http sp500_drop_alert \
          --schedule='30 9-15 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='America/New_York' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "SPY", "index_name": "S&P 500 (SPY)", "alert_type": "ath_drop", "threshold_percent": 30.0}'
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  # Cloud Scheduler Job for MSCI World ATH Drop Alert
  - name: 'gcr.io/cloud-builders/gcloud'
    args: [
      'scheduler', 'jobs', 'update', 'http', 'msci_drop_alert',
      '--schedule', '30 9-15 * * 1-5',  # Runs at the 30th minute of every hour from 9 AM to 3 PM (inclusive)
      '--uri', 'https://europe-west3-trading-436516.cloudfunctions.net/index_alert',
      '--http-method', 'POST',
      '--time-zone', 'America/New_York',
      '--location', 'europe-west3',
      '--oidc-service-account-email', '1098661711782-compute@developer.gserviceaccount.com',
      '--message-body', '{"index_symbol": "URTH", "index_name": "MSCI World", "alert_type": "ath_drop", "threshold_percent": 30.0}'
    ]
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  # Cloud Scheduler Job for URTH 255-day SMA crossing alert (try update first, then create if needed)
  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gcloud scheduler jobs update http urth_255sma_alert \
          --schedule='15 9-15 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='America/New_York' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "URTH", "index_name": "iShares MSCI World ETF", "alert_type": "sma_crossing", "sma_period": 255, "noise_threshold": 1.0}' \
        || gcloud scheduler jobs create http urth_255sma_alert \
          --schedule='15 9-15 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='America/New_York' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "URTH", "index_name": "iShares MSCI World ETF", "alert_type": "sma_crossing", "sma_period": 255, "noise_threshold": 1.0}'
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

  # Cloud Scheduler Job for SPY 200-day SMA crossing alert (try update first, then create if needed)
  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gcloud scheduler jobs update http spy_200sma_alert \
          --schedule='15 9-15 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='America/New_York' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "SPY", "index_name": "S&P 500 (SPY)", "alert_type": "sma_crossing", "sma_period": 200, "noise_threshold": 1.0}' \
        || gcloud scheduler jobs create http spy_200sma_alert \
          --schedule='15 9-15 * * 1-5' \
          --uri='https://europe-west3-trading-436516.cloudfunctions.net/index_alert' \
          --http-method='POST' \
          --time-zone='America/New_York' \
          --location='europe-west3' \
          --oidc-service-account-email='1098661711782-compute@developer.gserviceaccount.com' \
          --message-body='{"index_symbol": "SPY", "index_name": "S&P 500 (SPY)", "alert_type": "sma_crossing", "sma_period": 200, "noise_threshold": 1.0}'
    waitFor: ['-'] # start in parallel and actually needs to be set, otherwise deployment is sequential

options:
  logging: CLOUD_LOGGING_ONLY  # Send logs to Cloud Logging only