functions-framework==3.*
Flask==2.3.2  # For handling HTTP requests in Cloud Functions
google-cloud-secret-manager==2.7.2
python-dotenv==1.0.0
requests
pandas-market-calendars
google-cloud-firestore
pandas  # For SMA calculations from Alpaca data
alpaca-py>=0.21.0         # Alpaca Trading API SDK (modern version)
gspread>=5.12.0           # Google Sheets API
oauth2client>=4.1.3       # Google auth (service account credentials)
# torch/transformers/beautifulsoup4 entfernt 2026-09-21 mit dem Regime-SSO-
# Retirement: FinBERT war nur von daily_regime_check erreichbar, der
# S&P-500-Scrape nur von compute_market_breadth. Damit faellt die fn-heavy-
# Sonderbehandlung weg und jedes Image geht von 2,89 GB zurueck auf ~71 MB.
# Wenn je wieder eine schwere Abhaengigkeit dazukommt: NICHT hier eintragen,
# ohne den zweistufigen Source-Tree in cloudbuild.yaml wieder aufzubauen.
